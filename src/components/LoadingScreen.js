import styled from "styled-components";

const LoadingScreen = () => {
  return <LoadingScreenWrapper></LoadingScreenWrapper>;
};

const LoadingScreenWrapper = styled.div`
  
`;

export default LoadingScreen;
